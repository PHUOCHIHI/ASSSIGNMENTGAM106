@model Minecraft.Models.Vehicle
@{
    ViewData["Title"] = "Sửa phương tiện";
}

<h1 class="mb-4">Sửa phương tiện</h1>

<div class="card shadow-sm mb-4">
    <div class="card-body">
        <form asp-action="EditVehicle" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="VehicleId" />
            <div class="mb-3">
                <label class="form-label">Tên</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label class="form-label">Ảnh (URL)</label>
                <input asp-for="Image" class="form-control" />
            </div>
            <div class="mb-3">
                <label class="form-label">Giá trị (XP)</label>
                <input asp-for="Value" class="form-control" />
                <span asp-validation-for="Value" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label class="form-label">Loại</label>
                <select asp-for="Type" class="form-select" asp-items="Html.GetEnumSelectList<Minecraft.Models.VehicleType>()"></select>
                <span asp-validation-for="Type" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Lưu</button>
            <a asp-action="Vehicles" class="btn btn-secondary ms-2">Huỷ</a>
        </form>
    </div>
</div>
